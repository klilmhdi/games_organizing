import 'package:flutter/cupertino.dart';

class CardChallengeView extends StatelessWidget {
  const CardChallengeView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Card challenge"),);
  }
}
